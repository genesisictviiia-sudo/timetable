/**
 * Legacy Excel/PDF import is no longer used for Substitutions.
 * Substitution data is built at runtime from General Settings + generated Time Table.
 */
console.log("Skipping legacy Excel/PDF school data build (not used by the app).");
